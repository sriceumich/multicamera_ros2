import { ShaderNodeObject } from "three/tsl";
import { Node } from "/static/js/three/build/three.webgpu.js";

export const motionBlur: (
    inputNode: Node,
    velocity: Node,
    numSamples?: Node,
) => ShaderNodeObject<Node>;
